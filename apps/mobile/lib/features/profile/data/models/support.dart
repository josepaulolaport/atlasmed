// ── Support item ─────────────────────────────────────────────
class SupportItem {
  final String label;
  final String? sub;
  final String kind;

  const SupportItem({required this.label, this.sub, required this.kind});
}
