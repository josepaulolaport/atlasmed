// ── Recent activity ──────────────────────────────────────────
class RecentActivity {
  final String kind;
  final String title;
  final String detail;
  final String when;

  const RecentActivity({
    required this.kind,
    required this.title,
    required this.detail,
    required this.when,
  });
}
