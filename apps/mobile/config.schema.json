{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "AtlasMed Mobile App Configuration Schema",
  "type": "object",
  "properties": {
    "API_BASE_URL": {
      "type": "string",
      "minLength": 1,
      "description": "Base URL da API backend"
    },
    "MAPBOX_ACCESS_TOKEN": {
      "type": "string",
      "minLength": 1,
      "description": "Token público do Mapbox usado pelo mapa nativo"
    },
    "DEBUG_FIXED_LATITUDE": {
      "type": "string",
      "description": "Latitude fixa opcional para desenvolvimento (ex.: Rio -22.9068)"
    },
    "DEBUG_FIXED_LONGITUDE": {
      "type": "string",
      "description": "Longitude fixa opcional para desenvolvimento (ex.: Rio -43.1729)"
    }
  },
  "required": [
    "API_BASE_URL",
    "MAPBOX_ACCESS_TOKEN"
  ]
}
